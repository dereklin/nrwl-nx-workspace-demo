import { BackgroundColorRandomizer } from './background-color-randomizer';

describe('BackgroundColorRandomizer', () => {
  it('should work', () => {
    expect(new BackgroundColorRandomizer()).toBeDefined();
  });
});
