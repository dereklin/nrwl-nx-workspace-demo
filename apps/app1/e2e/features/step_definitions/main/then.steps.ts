import { Then } from 'cucumber';

Then('I should see the cost analysis chart', callback => {
  // Write code here that turns the phrase above into concrete actions
  callback();
});

Then('there should be a suggestion message', callback => {
  // Write code here that turns the phrase above into concrete actions
  callback();
});
